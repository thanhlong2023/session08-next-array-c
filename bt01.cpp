#include <bits/stdc++.h>
#define lenght sizeof(arr) / sizeof(arr[0])
int main()
{
    int arr[] = {1, 2, 3, 4, 5, 6, 7};
    for (int i = 0; i < lenght; i++)
    {
        printf("%d ", arr[i]);
    }
    return 0;
}